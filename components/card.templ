package components

templ Card(title string) {
	<div class="bg-bg-light rounded-lg p-4 md:p-6 mb-4">
		if title != "" {
			<h2 class="text-text-muted text-xs uppercase tracking-wider mb-4">{ title }</h2>
		}
		{ children... }
	</div>
}
