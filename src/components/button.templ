package components

templ SubmitButton(text string) {
	<button
		type="submit"
		class="mt-6 px-6 py-4  text-text cursor-pointer border border-border hover:bg-bg duration-200 ease-in-out font-bold uppercase tracking-widest rounded-sm"
	>
		{ text }
	</button>
}
