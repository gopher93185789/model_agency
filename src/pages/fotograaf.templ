package pages

import "github.com/gopher93185789/model_agency/pkg/types"

templ Fotograaf(
	data []types.ModelOverviewInfo,
) {
	for _, model := range data {
		<p class="text-text text-2xl font-bold">{model.Name}</p>
	}
}
